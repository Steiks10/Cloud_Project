// Contenido generado automáticamente por el Dockerfile durante la construcción de la imagen

export const environment = {
  backend_host: '172.11.15.22'  // El valor aquí puede variar según la variable de entorno BACKEND_HOST definida en el Dockerfile
};
